[project]
name = "feature-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "compiler-provenance",
]

[tool.uv.sources]
compiler-provenance = { path = "../compiler" }
